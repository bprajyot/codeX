apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: codearena-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: codearena.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: codearena-frontend
                port:
                  number: 80